# Scikit-Learn Playbook: dependencies
# Environment: conda `base` (Anaconda), Python 3.13.5. Versions below are the
# ones every notebook was actually run and tested with.
# To recreate elsewhere:
#   conda create -n sklearn_playbook python=3.13 && conda activate sklearn_playbook
#   pip install -r requirements.txt
#
# data/get_data.py also needs LibreOffice (soffice) to convert the credit .xls,
# but only if you rebuild the raw data; the CSVs are already committed.

scikit-learn==1.6.1
statsmodels==0.14.4
pandas==2.2.3
numpy==2.1.3
scipy==1.15.3
matplotlib==3.10.0
ipykernel==6.29.5
nbconvert==7.16.6
